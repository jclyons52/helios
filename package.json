{
	"scripts": {
		"build": "lerna run build --scope $MODULE_NAME --include-filtered-dependencies --stream",
		"start": "lerna run start --scope $MODULE_NAME --include-filtered-dependencies --parallel",
		"test": "lerna run test --scope $MODULE_NAME --include-filtered-dependencies  --stream"
	},
	"devDependencies": {
		"lerna": "^2.10.1"
	}
}
